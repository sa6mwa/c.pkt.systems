interface:
  display_name: "pkt.systems CMake Lifecycle"
  short_description: "Local C/CMake lifecycle automation"
  default_prompt: >-
    Use $pkt-systems-cmake-lifecycle to classify the repository, preserve bespoke
    workflows, implement the requested lifecycle task, run the appropriate local
    gates, and report exact outcomes.
